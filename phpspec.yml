suites:
    main:
        spec_path: tests
        namespace: Cerbero\FluentApi
        psr4_prefix: Cerbero\FluentApi
extensions:
    - PhpSpec\Extension\CodeCoverageExtension
code_coverage:
    format:
      - clover
    output:
      clover: coverage.clover
